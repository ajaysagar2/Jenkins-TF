pipeline {
    agent any

    environment {
        NAME = 'ajaysagar'
        LASTNAME = 'Musini'
    }

    stages {
        stage('Build') {
            steps {
                sh 'echo $NAME $LASTNAME'
            }
        }
    }
}
